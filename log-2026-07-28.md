# Log 2026-07-28

## 1. 專案結構閱讀

首次讀取 `LaneMapLeader` 專案，了解其架構：

- **技術棧**: React 18 + TypeScript + Vite，MapLibre GL（react-map-gl），Tailwind CSS
- **外部服務**: OpenFreeMap（地圖）、OSRM（路線規劃）、Nominatim（地理搜尋）
- **項目結構**:
  ```
  src/
  ├── App.tsx              # 主應用（GPS/地圖/路線/導航整合）
  ├── context/             # NavigationContext - 導航狀態管理
  ├── components/
  │   ├── map/             # MapView, RoadLayer, RouteLayer, GPSMarker
  │   ├── navigation/      # SearchBar, RoutePanel, NavigationOverlay
  │   ├── lane/            # LaneCard, LaneArrow, LaneIcons
  │   └── ui/              # Controls, SpeedPanel, Compass, LoadingSpinner
  ├── hooks/               # useGeolocation, useOrientation, useRoadMatching, useRouting...
  ├── services/            # OSRM, Nominatim, roads
  ├── types/               # navigation, roads, routing 型別
  └── utils/               # lanes, geo, formatters, navigation, routePreferences（含測試）
  ```
- **道路資料**: `public/data/macau/roads.json`、`lane_overrides.json`、CLI 工具鏈

---

## 2. README 多語言版本

### 訴求
為 README 建立繁體中文和英文版本，預設使用簡體中文，並在 README 上建立導向其他語言的連結。

### 實作
- 建立 `README.zh-Hant.md`（繁體中文）
- 建立 `README.en.md`（英文）
- 更新 `README.md`（簡體中文），在頂部加入語言導航列：
  ```
  **简体中文** · [繁體中文](README.zh-Hant.md) · [English](README.en.md)
  ```

### 後續調整
- 第一次調整：移除所有"澳門"/"Macau"字樣，因為項目定位為全球通用工具
- 第二次調整：將道路資料工具章節的示例加上"以澳門為例"標註，因為範例命令使用澳門數據。同時將 `public/data/macau/osm_fr.json` 改為泛用的 `public/data/<city>/osm_fr.json`

---

## 3. 多語言 i18n 支援（簡體/繁體/英文 + 自動識別）

### 訴求
網頁界面要準備簡體、繁體和英文的版本使用，自動識別語言。

### 設計決策

採用**模組級全域狀態 + React Context**雙軌架構：

- **模組級 `t()` 函數**：可在 React 元件外的 hooks/services/utils 中使用
- **React `useT()` hook**：僅在元件內使用，保證語言切換時重新渲染
- **自動偵測**：從 `navigator.language` 判斷

語言對應規則：
| 瀏覽器語言 | 應用語言 |
|------------|----------|
| `zh-TW`, `zh-HK`, `zh-MO`, `zh-Hant` | `zh-Hant`（繁體） |
| 其他 `zh-*` | `zh-CN`（簡體） |
| `en-*` | `en`（英文） |
| 其他 | `zh-CN`（預設） |

### 新增檔案

| 檔案 | 用途 |
|------|------|
| `src/i18n/core.ts` | 模組級狀態：`t()`、`setLanguage()`、`getLanguage()`、`detectLanguage()` |
| `src/i18n/translations.ts` | 三語對照翻譯表（約 85 個 key），一個檔案便於對照 |
| `src/i18n/index.tsx` | React Provider `I18nProvider`、`useI18n()`、`useT()` hooks |

### 修改檔案（共 19 個檔案）

#### 入口層
- **`src/main.tsx`**: 在 `<ErrorBoundary>` 外包裹 `<I18nProvider>`

#### 工具函數層（改用 `import { t } from '../i18n/core'`）
- **`src/utils/formatters.ts`**: `formatDuration()` 和 `getManeuverText()` 使用 `t()` 替換硬編碼中文；`isBridgeRoad()` 正則加入英文橋梁詞彙（bridge/viaduct/overpass/flyover）
- **`src/utils/navigation.ts`**: `parseSteps()` 中使用 `t()` 替換"到達"、"往"等字串
- **`src/services/roads.ts`**: 錯誤訊息使用 `t()` 替換硬編碼中文

#### Hooks 層
- **`src/hooks/useGeolocation.ts`**: 5 個 GPS 錯誤訊息改用 `t()`
- **`src/hooks/useRouting.ts`**: 2 個路線錯誤訊息改用 `t()`
- **`src/hooks/useGeocoding.ts`**: 搜尋失敗訊息改用 `t()`

#### React 元件層（改用 `useT()` hook）
- **`src/App.tsx`**: 13 處硬編碼中文 → `t('key')`，包括載入文字、起點終點按鈕、到達提示、導航按鈕等
- **`src/components/ErrorBoundary.tsx`**: `t('error.*')` 替換錯誤標題、說明、技術細節、重新載入按鈕
- **`src/components/ui/LoadingSpinner.tsx`**: 預設訊息改用 `t('loading.default')`
- **`src/components/ui/SpeedPanel.tsx`**: 精度、順向/反向文字改用 `t()`
- **`src/components/ui/Compass.tsx`**: title 屬性改用 `t('compass.*')`
- **`src/components/ui/Controls.tsx`**: title/aria-label 改用 `t('controls.*')`
- **`src/components/navigation/SearchBar.tsx`**: placeholder 改用 `t('search.placeholder')`
- **`src/components/navigation/RoutePanel.tsx`**: 路線選擇、偏好、推薦/替代路線等 12 處改用 `t()`
- **`src/components/navigation/NavigationOverlay.tsx`**: 導航指示、道路名稱、剩餘距離等 6 處改用 `t()`
- **`src/components/lane/LaneCard.tsx`**: 車道載入、方向、車道數、距離等 8 處改用 `t()`
- **`src/components/lane/LaneArrow.tsx`**: 車道編號改用 `t('lane.laneNumber')`

#### 測試
- **`src/utils/formatters.test.ts`**: 在 `beforeAll` 中呼叫 `setLanguage('zh-Hant')`，因為既有測試斷言使用繁體中文

### 翻譯 Key 設計

模組化命名，用 `.` 分隔：
```
app.*           App 層級文字（錯誤、載入、按鈕）
error.*         ErrorBoundary 文字
loading.*       LoadingSpinner
speed.*         SpeedPanel
compass.*       Compass
controls.*      Controls
search.*        SearchBar
route.*         RoutePanel
nav.*           NavigationOverlay
lane.*          LaneCard / LaneArrow
geo.*           GPS 相關錯誤
route.*         路線相關錯誤（與上述 route.* 不同語境）
search.*        搜尋相關錯誤
road.*          道路資料錯誤
format.*        格式化（分鍾、小時）
maneuver.*      駕駛操作（轉彎、迴轉、直行等）
navUtil.*       導航工具函數
```

每個 key 在 `translations.ts` 中有三個語言的對照值。

### 驗證

- **TypeScript**: `npx tsc --noEmit` — 0 errors
- **Lint**: `npm run lint` — 0 errors, 3 warnings（`react-refresh/only-export-components`，與既有警告一致，非致命）
- **Tests**: `npm test -- --run` — 55/55 passed
- **Build**: `npm run build` — 成功

---

## 4. 更新 AGENTS.md

在既有 `AGENTS.md` 中新增 `i18n` 章節，記錄：

- 支援語言和自動偵測規則
- 翻譯檔案位置 (`src/i18n/translations.ts`)
- 兩種 API：模組級 `t()` vs React `useT()` hook
- 新增字串的標準流程
- 測試模式（`setLanguage` 後斷言）
- 非致命 lint 警告清單更新（加入 `i18n/index.tsx`）
- `main.tsx` 描述更新（加入 `<I18nProvider>` 包裝層）
